# ####################################################################
# ### Logger You can filter anything between the []'s in the logger ##
# ####################################################################

# Log Severities: notset, debug, info, warn, warning, error, fatal, critical

default: error
logs:
  homeassistant.components.sensor: fatal
  root: warning
